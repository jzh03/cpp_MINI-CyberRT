# 仓库文档维护约定

本约定适用于整个仓库。沿用现有名称：`readme.md` 指 `README.md`，`AGNETS.md` 指 `AGENTS.md`，
`bulid` 指 `build`；不要新建拼写或大小写不同的副本。

## 文档分工与写法

| 文档 | 主要内容 |
| --- | --- |
| [README.md](README.md) | 功能、API 行为、架构与同步约定、兼容性及使用边界 |
| [example/TESTING.md](example/TESTING.md) | 测试覆盖、构建入口、依赖、运行参数、超时、清理和验证边界 |
| [example/testlog.md](example/testlog.md) | 完整实际命令、环境、执行结果、失败及复验、日志位置 |

除测试日志和原始 `.log` 外，文档保持简洁易懂：先讲用途和入口，再讲必要的原理与边界。
一段说明一件事，并列内容用简表或列表；避免重复的背景、修复过程和实现细节。
同一说明以职责对应的文档为主，其他页面用相对链接引用。精简时保留必要参数、失败条件和使用限制。

简洁不以删减行数为目标。架构和模块文档应充分解释关键链路、对象关系、线程及所有权边界，
适当配 Mermaid 流程图、时序图或状态图，并用短段落说明读图重点。
维护已有的有效配图；合并或删除前确认其解释作用已被保留，不因压缩篇幅把图和关键过程一并删去。
图中的调用顺序、分支和生命周期须与源码一致；简单规则或并列比较用文字、表格即可。

## 更新规则

- 功能变化更新 README，测试程序或运行方式变化更新 TESTING；实际构建或运行后更新 testlog。
  纯文档、排版、索引、维护约定及静态检查不写入 testlog，也不为此重跑中间件测试。
- TESTING 中是可复用示例，testlog 中是已执行记录，两者必须明确区分。实际记录保留日期、
  分支/HEAD、工作目录、环境变量、BUILD_DIR、sanitizer 选项、目标/过滤条件、退出状态和日志路径。
  变量须给出定义，不能用省略号代替必要参数。
- 分开记录“仅构建、测试通过、失败、超时、环境阻止启动”。未执行不得写通过，文档或静态检查不算程序测试。
  保留历史失败与复验，不能用后来的成功覆盖失败；历史命令缺失就标明缺失，不按指南推测补写。
- 同机 INTRA、跨进程 SHM、同机强制 RTPS、受控 host 元数据路由、真实跨主机验证分别说明，不能扩大结论。
- 每次新增、补充或修正 testlog 正文，必须同步更新顶部“查找记录”；新一轮验证追加正文和入口，
  同一轮补充则更新原记录及摘要。每条索引包含日期、简明摘要和章节链接。
- 索引按改动模块分组，组内按实际时间从早到晚，同日按正文顺序。跨模块归主要模块，摘要注明相关范围。
  日期未记录的条目放在对应组末，不推测日期。
- 文档须与源码、构建入口一致。检查本地链接、测试目标、`git diff --check`，并确认 testlog 每条正文记录
  都有索引入口，分类与排序正确；改标题时保留或同步更新已有锚点。

## 编译产物归档

| 产物 | 目录 |
| --- | --- |
| 普通可执行文件、`.o/.d/.so/.a`、生成头文件和构建缓存 | `example/build/` 或其配置子目录 |
| benchmark、sanitizer、独立验证 | 如 `example/build/benchmark/`、`example/build/asan/`、`example/build/tsan/` |
| class-loader 测试插件 | 相应 `BUILD_DIR/class_loader/` |
| 通信 Demo | `example/demo/build/` |

默认 `BUILD_DIR` 为 `example/build/`，覆盖值也须遵守上表。不同优化参数、ABI、sanitizer 使用独立目录，不能混用对象；
不在源码目录、根目录、`/tmp`、`example/build-*` 平级目录或 `class_loader/test/` 留编译产物。
清理前确认目标只含可再生成产物且无进程使用，源码、测试记录和日志保留。
移动构建目录后须清理并重建，不能复用含旧绝对路径的依赖文件。
脚本和使用文档采用新目录规则；历史 testlog 保留当时路径，不为迁移改写旧命令。

## 日志文件归档

- 所有 `.log`（包括运行、构建、测试、临时诊断输出）统一写入根目录 `log/`，可按用途建子目录。
  不写在根目录散落位置、example、构建目录或 `/tmp`。轮转 `.log.*` 也留在同一日志目录。
- `log/` 含 Logger 源码，不能整体忽略、删除或覆盖；仅日志及轮转文件由 Git 忽略，源码继续跟踪。
  `make clean` 保留日志。
- `Init()`、`Logger_Init()`、`Logger::open()` 的调用者只提供文件名；传入路径时只保留末尾名称并补齐 `.log`。
  路径和轮转由 Logger 管理，新增写入入口同样遵守。
- `CMW_PATH` 指向项目根目录；未设置时，example/Makefile 产物使用编译时项目根目录。
  其他构建方式须定义 `CMW_PROJECT_ROOT` 或设置 `CMW_PATH`。定位、建目录或打开失败须明确失败，不回退当前目录。
- 迁移日志须保留内容，不覆盖同名文件。历史记录保留原路径，另记迁移位置；`.log` 不能代替 testlog 的完整命令与结果摘要。
