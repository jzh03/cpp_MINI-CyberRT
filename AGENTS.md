# 仓库文档维护约定

本文件适用于整个仓库。文档按以下职责维护，沿用已有文件名和路径；
用户提到的 `readme.md` 对应根目录的 `README.md`，不要另建大小写不同的副本。

| 文档 | 应记录的内容 |
| --- | --- |
| [README.md](README.md) | 功能性更新：新增或修复的功能、API 行为、架构与同步约定、兼容性要求、使用边界。 |
| [example/TESTING.md](example/TESTING.md) | 测试程序的功能介绍和使用指南：测试覆盖、构建入口、依赖、运行方法、参数、超时、资源清理和验证边界。 |
| [example/testlog.md](example/testlog.md) | 测试的完整实际命令、执行记录和结果：日期、分支/HEAD、环境、构建目录、测试范围、退出状态、失败原因、复验结果和日志位置。 |

## 更新规则

- 功能发生变化时更新 README；测试程序或运行方式发生变化时更新 TESTING；
  实际执行构建、测试或文档检查后，将命令与结果记录到 testlog。
- TESTING 中的命令是可复用的操作示例，须与 testlog 中已执行的命令明确区分。
  testlog 中保留实际工作目录、环境变量、BUILD_DIR、sanitizer 选项、测试目标
  和过滤条件；命令使用变量时应给出定义，不用省略号代替必要参数。
- 未执行的测试不得记录为通过；仅构建、测试通过、失败、超时和环境阻止启动
  应分别说明。文档更新没有运行程序测试时，也应明确记录。
- 保留历史失败与复验记录，不用后来的成功覆盖此前失败。历史命令缺失时标注
  缺失，不根据操作指南推测补写成已执行命令。
- 同机 INTRA、跨进程 SHM、同机强制 RTPS、受控 host 元数据路由和真实跨主机
  验证必须区分，不能扩大测试结论。
- 同一说明以对应职责的文档为主，其他文档使用相对链接引用，避免重复维护。
- 文档修改保持与实际源码和构建入口一致；检查本地链接、测试目标和
  `git diff --check`。只改文档时无需为此重跑中间件测试。

## 日志文件归档

- 项目产生的所有 `.log` 文件统一保存在项目根目录的 `log/` 下，包括运行日志、
  构建/测试输出重定向和临时诊断日志。可按用途或原始相对路径建立子目录；
  不要在根目录、example、构建目录或 `/tmp` 另写 `.log`。
- `log/` 已包含 logger 源码，不要忽略、删除或覆盖整个目录。仅日志文件及
  `.log.*` 轮转文件由 Git 忽略，源码继续跟踪；`make clean` 保留日志。
- 程序通过 `Init()`、`Logger_Init()` 或 `Logger::open()` 写日志，路径统一由
  Logger 管理。调用者仅提供文件名；传入路径时只保留末尾文件名，并补齐 `.log`。
  日志轮转仍保存在同一目录。新增写入入口也必须遵守这一规则。
- `CMW_PATH` 应指向项目根目录；未设置时，example/Makefile 构建的程序使用
  编译时项目根目录。其他构建方式须定义 `CMW_PROJECT_ROOT` 或设置 `CMW_PATH`。
  无法定位根目录、创建目录或打开文件时应明确失败，不退回当前工作目录。
- 迁移已有日志时保留内容，同名文件不得覆盖；历史执行记录保留当时实际路径，
  另行记录迁移位置。日志文件不代替 testlog.md 中的完整实际命令与结果摘要。
